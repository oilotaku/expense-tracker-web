# Scan Report — expense-tracker-web

## 1. 總覽

- **時間**：2026-09-17 09:37（Asia/Taipei）
- **分區**：ENV / FE / BE / DB+PII / CACHE / SEC 全跑（`focus=all`，無舊報告可差異基準，第 0 章省略）
- **嚴重度統計**：🔴 4　🟠 4　🟡 9　🔵 5　⚪ 1
- **結論**：🔴 與 🟠 皆非 0，**不可 self-approve**。本次掃描後同一工作階段內已修正並驗證通過：`R-ENV-003`（`.gitignore` 萬用規則）、`R-PII-001` × 2（`admin_service.py` 移除 log 內明文 email）、`R-DB-008`（複合索引補 `postgresql_concurrently`）、`R-SEC-002`（`/auth/login`/`/auth/register` 加 rate limit）。`R-ENV-006`（`.env.test.local` 曾進 git 歷史）評估後**刻意不處理**：查證內容為測試假值 `SECRET=x`，非真實密鑰，改寫歷史需 force-push 破壞 public repo 既有 clone，代價大於實際風險，根因已由 `R-ENV-003` 的修正阻斷。其餘 2 條 🟠 + 9 條 🟡 + 5 條 🔵 + 1 條 ⚪ 仍待後續處理；另外實作 `R-SEC-002` 過程中發現一項新的 AD（`AD-002`：既有 pytest `Event loop is closed` flakiness，詳見第 6 章）。

## 2. 專案摘要

- **目標**：個人記帳網站（多帳戶、收支記錄、週期性交易、預算追蹤、資產/負債總覽、統計圖表），單人開發、Harness Engineering 規範驅動。
- **技術棧對照**：Next.js 16 App Router + TS strict + RTK Query + Tailwind v4／FastAPI + SQLAlchemy 2 async + Pydantic 2 + Alembic + uv／PostgreSQL 18（asyncpg）／Redis 8（報價快取）／Docker Compose／GitHub Actions。與 `rules/00-core/00-overview.md` CORE-015 棧鎖定一致。
- **目錄結構**：`backend/app/{api,core,models,repositories,schemas,services,clients,utils}` 分層清楚；`frontend/src/{app,components,hooks,lib/api,utils}`；`docs/{Arch,Rules,Tasks}` 依 Harness 慣例分工。
- **Task 進度**：v1.0.0、v1.1.0 皆已完成（40/40 + 補洞 task），無開放中的下一版任務資料夾。`docs/Tasks/v1.1.0/fixed.md` 累積 §1–§18，皆已標記處理完畢。
- **本次掃描時工作樹狀態**：有一組尚未 commit 的變更，是使用者要求實作 `docs/Arch/adr/0004-read-path-capacity-planning.md`（ADR-0004）的兩項待辦：
  1. `backend/app/models/transaction.py` + 新 migration `2026_09_17_1000-add_transactions_user_date_index.py`：補 `(user_uid, transaction_date DESC, uid DESC)` 複合索引
  2. `backend/app/schemas/dashboard.py`：`DashboardDateRangeFilter` 新增 366 天查詢區間上限
  兩項改動已跑過 migration upgrade→downgrade→upgrade round-trip、`ruff check`、`ruff format --check`、`mypy app`、`pytest tests/api/test_dashboard.py tests/api/test_transactions.py`，全數通過（52 tests passed）。掃描期間 DB sub-agent 額外抓到複合索引 migration 漏加 `postgresql_concurrently`，已於本次工作階段修正並重驗（見下方 R-DB-008）。掃描後使用者要求修復 4 個 Critical，其中 3 個（`R-ENV-003`、`R-PII-001` × 2）已完成並重跑驗證，加測 `tests/api/test_admin.py` 後共 62 tests passed，`ruff`/`mypy` 依然全乾淨。
- **完成度**：核心記帳功能（v1.0.0）+ 視覺改版（v1.1.0）皆已上線；本次新增的效能項目屬 ADR-0004 後續實作，尚未 commit。

## 3. 詳細發現

### 🔴 [R-ENV-006 → CORE-030] `.env.test.local` 曾被 commit 進版控歷史（**評估後刻意不處理**）
- 檔案：`(git history)` commit `0c4b9cf`「(AI) Add: test」新增 `.env.test.local`，commit `a48d5f7`「(AI) Fix: 移除測試誤 commit 的 .env.test.local」刪除
- 內容：該檔雖已在下一個 commit 移除，但仍永久留在 `git log --all` 歷史裡。查證 `git show 0c4b9cf:.env.test.local` 實際內容為 `SECRET=x`，是測試用假值，非真實密鑰。
- 處理狀態：**刻意不做歷史改寫**。用 `git filter-repo`/BFG 清歷史需要對這個已公開的 GitHub repo force-push，會讓所有既有 clone/fork 的 commit hash 全部作廢，代價遠大於一個假值曾經存在過的實際風險；根因已由下方 `R-ENV-003` 的修正阻斷再犯。若使用者之後改變決定，可再另外處理。
- 首次發現：2026-09-17

### 🔴 [R-ENV-003 → CORE-027] `.gitignore` 未涵蓋 `.env.*.local` 等變體（上一條的根因，**已修正**）
- 檔案：`.gitignore:20-26`
- 內容：用逐一列舉方式排除 `.env`/`.env.development`/`.env.staging`/`.env.production`，未涵蓋 `.env.test.local`、`.env.local`、`.env.*.local`；實測 `git check-ignore -v .env.test.local` 回傳 exit 1（未被忽略）——這正是上一條意外 commit 的根本原因，且今天再建一次同名檔案仍會被追蹤。
- 處理狀態：已改成跟 `.dockerignore` 一致的萬用寫法：`.env` + `.env.*` + `!.env.*.example`，一次涵蓋所有變體，不再逐一列舉環境名稱。
- 首次發現：2026-09-17（同日修正）

### 🔴 [R-PII-001 → DB-029/BE-053] `admin_service.py` 把管理員 email 明文寫進結構化日誌（刪除使用者，**已修正**）
- 檔案：`backend/app/services/admin_service.py:63`
- 內容：`logger.warning("admin deleted user", extra={"admin_email": admin_user.email, ...})` 把管理員的 email 明文塞進 log 欄位。
- 處理狀態：已改記 `admin_user.user_uid`（對外識別碼已足夠追溯身分），移除 `admin_email` 欄位。
- 首次發現：2026-09-17（同日修正）

### 🔴 [R-PII-001 → DB-029/BE-053] `admin_service.py` 把管理員 email 明文寫進結構化日誌（重設密碼，**已修正**）
- 檔案：`backend/app/services/admin_service.py:83`
- 內容：`reset_password` 的 `logger.warning` 同樣用 `extra={"admin_email": admin_user.email, ...}`。
- 處理狀態：同上，已改用 `user_uid` 取代 email。
- 首次發現：2026-09-17（同日修正）

### 🟠 [R-SEC-002 → BE-034/CACHE-022] `/auth/login`、`/auth/register` 無 rate limit（**已修正**）
- 檔案：`backend/app/api/v1/auth.py:40-50`
- 內容：全 repo 搜尋 slowapi/limiter/Limiter 均無結果，`pyproject.toml` 未裝任何限流套件；登入/註冊端點可被暴力破解密碼或大量灌註冊帳號。
- 處理狀態：新增 `backend/app/core/rate_limit.py`（依 CACHE-022 的 sorted set 滑動視窗，`redis` 不可用時回 503 `CACHE_UNAVAILABLE`，屬功能依賴型），以 FastAPI dependency 掛在 `/auth/login`（10 次/60 秒/IP）與 `/auth/register`（5 次/3600 秒/IP），超限回 429 並帶 `Retry-After`。`AppError`/`failure()` 加上可選 `headers` 支援此用途。測試新增 `tests/api/test_auth_rate_limit.py`（限流生效、Redis 不可用兩種情境），並修正 `tests/api/test_auth_pin.py` 的 `real_client` fixture（原本不 override 任何 dependency，會真的消耗掉限流額度、干擾其他測試）一併 no-op 掉這兩個限流依賴。全套件驗證 214 passed、0 failed（另有 8 個跟此改動無關的既有 `Event loop is closed` flakiness，詳見下方對應章節）。
- 首次發現：2026-09-17（同日修正）

### 🟠 [R-FE-008 → FE-037] 全站無 server-side 路由保護，只靠 client component
- 檔案：`frontend/src/components/AuthGuard.tsx:17-35`（全部 13 個受保護頁共用）
- 內容：路由保護只靠 client component 在 `useEffect` 裡打 `/auth/me` 後才導頁，沒有任何 server-side/edge 檢查，未登入者仍會先拿到整包受保護頁的 JS bundle。
- 修正：新增 `frontend/src/middleware.ts`，讀 httpOnly cookie 是否存在就先擋一次導向 `/login`（後端每個 API 仍是最終授權判斷，不解 JWT 判角色），`AuthGuard` 繼續做 UX 層 fallback。
- 首次發現：2026-09-17

### 🟠 [R-DB-008 → DB-056/DB-057] 新複合索引缺 `postgresql_concurrently`（**已於本次工作階段修正**）
- 檔案：`backend/alembic/versions/2026_09_17_1000-add_transactions_user_date_index.py`（對應 `backend/app/models/transaction.py` 的 `Index` 定義）
- 內容：本次為實作 ADR-0004 新增的複合索引 migration，原本用預設（鎖表）方式建索引，但 ADR-0004 自己記載「建議在正式對外開放前補上（`CREATE INDEX CONCURRENTLY` 可不鎖表施工）」，且 ADR 估算 1000 人×3 年規模下 `transactions` 約 300–900 萬列，正式環境跑這支 migration 會長時間鎖表擋寫入。
- 處理狀態：已改成 `postgresql_concurrently=True` 並用 `op.get_context().autocommit_block()` 跳出 `env.py` 預設的 `begin_transaction()`，重跑 migration round-trip（upgrade→downgrade→upgrade）+ `ruff`/`mypy`/`pytest` 全數通過。
- 首次發現：2026-09-17（同日修正）

### 🟠 [R-PII-003 → CORE-137] 使用者自助登入/改密碼/PIN 操作缺審計紀錄
- 檔案：`backend/app/services/auth_service.py:55-140`
- 內容：`login` / `login_with_pin` / `change_password` / `set_pin` / `change_pin` / `disable_pin` 完全沒有寫審計紀錄；`record_login` 只更新 `last_login_at`（model 註解自陳只記最後一次，非完整登入歷史）。對照 `admin_service.py` 對管理員操作都有 `logger.warning` 留痕，使用者自助動作反而沒有對應軌跡。
- 修正：比照 `admin_service` 模式，在這些操作成功（可考慮含失敗鎖定）時各記一筆不含明文密碼/PIN 的審計事件（例如 `logger.info("user login", extra={"user_uid": ...})`），或視需求另建獨立 audit_log 表持久化。
- 首次發現：2026-09-17

### 🟡 [R-FE-011 → FE-006] 交易清單項目元件未 memo
- 檔案：`frontend/src/components/TransactionList.tsx:282,332`
- 內容：未偵測到 React Compiler；`TransactionTableRow`/`TransactionCard` 是清單逐筆渲染元件但未包 `React.memo`，父層任何 state 變動都會重渲染整份清單。
- 修正：用 `React.memo` 包裝，props 已是原始值/穩定 setState function，可安全淺比較。
- 首次發現：2026-09-17

### 🟡 [R-FE-011 → FE-006] 帳戶卡片元件未 memo
- 檔案：`frontend/src/components/accounts/AccountCard.tsx:191`、`frontend/src/app/accounts/page.tsx:247`
- 內容：同上模式，`AccountCard` 於 `accounts.map` 中渲染，未 memo。
- 修正：同上，包 `React.memo`。
- 首次發現：2026-09-17

### 🟡 [R-FE-011 → FE-006] 分類 chip 元件未 memo
- 檔案：`frontend/src/components/categories/CategoryChip.tsx:184`、`frontend/src/app/categories/page.tsx:185`
- 內容：同上模式，`CategoryChip` 未 memo。
- 修正：同上，包 `React.memo`。
- 首次發現：2026-09-17

### 🟡 [R-FE-019 → CORE-047/FE-039] 後台管理頁日期時間未走共用時區格式化
- 檔案：`frontend/src/app/admin/page.tsx:89,94`
- 內容：全站其餘日期時間顯示都走 `frontend/src/utils/datetime.ts` 固定 `Asia/Taipei`，唯獨這兩處改用 `new Date(...).toLocaleDateString('zh-TW')` / `toLocaleString('zh-TW')`，未指定時區，會依瀏覽器本地時區顯示，與站內其他頁面不一致。
- 修正：改用 `formatDate`/`formatDateTime`（`utils/datetime.ts`）取代。
- 首次發現：2026-09-17

### 🟡 [R-FE-020 → FE-066] API 型別手寫，未接 OpenAPI codegen
- 檔案：`frontend/src/lib/api/authApi.ts:6-9`（同模式亦見 `accountsApi.ts`/`categoriesApi.ts`/`transactionsApi.ts` 等多檔）
- 內容：專案未接 openapi-typescript pipeline，找不到 `src/lib/api/schema.d.ts`，全部 endpoint 型別皆手寫，非 `components['schemas'][...]`（程式碼內已有註解自陳為已知技術債）。
- 修正：導入 openapi-typescript codegen 讀後端 `/api/openapi.json` 產生 `schema.d.ts`，逐步把手寫介面換成 `components['schemas']['X']`。
- 首次發現：2026-09-17

### 🟡 [R-BE-014 → BE-002] `DashboardService` 跳過 repository 層直接下 SQL
- 檔案：`backend/app/services/dashboard_service.py:104-119,147-174,200-216,242-258`
- 內容：直接用 `select()`/`self._db.execute()` 組 SQL 查詢，是本專案唯一一處 service 層直接碰 raw SQL 的地方（其他 service 皆透過對應 repository）。
- 修正：抽一個 `DashboardRepository` 封裝這些 `GROUP BY` 查詢；若團隊認定「彙總類查詢直接寫在 service 較合理」屬刻意例外，建議在 ADR 或程式碼註解明確記錄這個分層例外的理由（目前註解只解釋效能考量，沒解釋跳過 repository 層的理由）。
- 首次發現：2026-09-17

### 🟡 [R-BE-002 → BE-010] `auth.py` 多個 endpoint 用動詞而非資源名詞
- 檔案：`backend/app/api/v1/auth.py:40,53,86,99,111,123,128`
- 內容：`/auth/login`、`/auth/logout`、`/auth/change-password`、`/auth/login/pin` 等路徑用動作動詞而非資源名詞。
- 修正：若非刻意保留業界常見的 auth RPC 風格例外，可改資源化寫法（例如 `POST /auth/sessions` 表登入、`DELETE /auth/sessions` 表登出），否則建議在規範文件明確標註 auth 端點的例外。
- 首次發現：2026-09-17

### 🟡 [R-BE-002 → BE-010] 子路徑用動詞（`repay`/`run`）
- 檔案：`backend/app/api/v1/liabilities.py:120`（`/liabilities/{liability_uid}/repay`）、`backend/app/api/v1/internal.py:33`（`/internal/recurring/run`）
- 內容：子路徑用動詞而非名詞子資源。
- 修正：可考慮改為 `POST /liabilities/{id}/repayments`、`POST /internal/recurring-runs`；`internal/recurring/run` 屬內部排程觸發 API 已有文件豁免說明，嚴重度可視為最低。
- 首次發現：2026-09-17

### 🟡 [R-LOG-005 → BE-055/CORE-045] 缺結構化 log 與 request_id
- 檔案：`backend/app/main.py`（全檔未見任何 logging 設定）、`backend/app/core/exceptions.py:52`、`backend/app/services/net_worth_service.py:144`、`backend/app/services/admin_service.py:61,81`
- 內容：全專案沒有 `logging.basicConfig`/`dictConfig`/結構化 log 設定，也沒有 request_id 中介層，`logger.exception`/`logger.warning` 皆用預設 formatter（無 ISO8601 帶 offset 時間戳、無 request_id 可關聯單一請求）。
- 修正：在 `app/main.py` 或獨立 `app/core/logging.py` 設定 JSON formatter（含 ISO8601+offset 時間、request_id、method/path），並加一個產生/透傳 `X-Request-Id` 的 middleware。
- 首次發現：2026-09-17

### 🔵 [R-ENV-007 → CORE-019] uv 版本字串硬寫三處
- 檔案：`docker/backend/Dockerfile:6`、`.github/workflows/ci.yml:78`、`.github/workflows/ci.yml:107`
- 內容：`ghcr.io/astral-sh/uv:0.12.5` 在三個地方各寫一次字面值，跟同專案其他版本（NODE_VERSION/PYTHON_VERSION/POSTGRES_VERSION/REDIS_VERSION）都走 `.env`/版本表不同，將來升版容易漏改。
- 修正：把 uv 版本納入 `rules/00-core/01-versions.md` 版本表統一管理，Dockerfile 與 workflow 都改成引用同一個來源。
- 首次發現：2026-09-17

### 🔵 [R-GIT-002 → CORE-087] 一筆歷史 commit message 不符格式
- 檔案：`(git history)` commit `2e97858`
- 內容：commit message「chore: create root README.md with project overview」不符合 `^(\(AI\) )?(Add|Modify|Fix|Refactor|Docs): ` 格式。
- 修正：之後同類 commit 一律改用規範格式；此筆歷史 commit 不需要改寫歷史，僅供 PR review checklist 提醒。
- 首次發現：2026-09-17

### 🔵 [R-FE-017 → FE-020] 錯誤邊界未接 Sentry 上報
- 檔案：`frontend/src/app/error.tsx:1-19`、`frontend/src/app/global-error.tsx`
- 內容：`package.json` 未裝 `@sentry/nextjs`，兩個錯誤邊界皆只有註解「接 Sentry 時依 FE-020…」，目前完全沒有上報機制。
- 修正：安裝 `@sentry/nextjs`，在兩個 error boundary 的 `useEffect` 內呼叫 `Sentry.captureException(error)`。
- 首次發現：2026-09-17

### 🔵 [R-LOG-006 → CICD-081] 缺 `/api/v1/version` 端點
- 檔案：`backend/app/api/v1/health.py`（整檔）、`backend/app/api/v1/__init__.py:20-32`
- 內容：只有 `GET /api/v1/health`，找不到 `GET /api/v1/version`。
- 修正：補一個回傳版本號/commit hash 的 `/api/v1/version` 端點。
- 首次發現：2026-09-17

### 🔵 [R-FE-004 → FE-009] 全站無 i18n 框架（架構層級）
- 檔案：全站（`frontend/src/app/**/*.tsx`、`frontend/src/components/**/*.tsx`）
- 內容：`package.json` 未見任何 i18n 套件，全部使用者可見文字皆直接寫死繁中字面值；屬架構層級、非個別檔案疏漏，數量過大不逐條列出。
- 修正：若未來有多語需求，需先導入 i18n 框架並建立 key 對照表，屬專案級決策非單檔修正。
- 首次發現：2026-09-17

### ⚪ [R-FE-002 → CORE-112] `layout.tsx` 的 `dangerouslySetInnerHTML`（低風險，僅記錄）
- 檔案：`frontend/src/app/layout.tsx:30`
- 內容：內容是編譯期常數字串 `THEME_INIT_SCRIPT`（防 FOUC 用的內嵌 script），非任何使用者輸入或後端資料，實際 XSS 風險極低。
- 修正：不需要立即處理；若之後改成動態內容需重新評估。
- 首次發現：2026-09-17

## 4. 修正優先序

- **立刻**（🔴，資料外洩/機密風險）：
  1. ~~`.gitignore` 補 `.env.*` 萬用規則~~（R-ENV-003，已修正）
  2. ~~`admin_service.py:63,83` 拿掉 log 裡的 `admin_email`~~（R-PII-001 × 2，已修正）
  3. `.env.test.local` 歷史內容（R-ENV-006）——評估後刻意不清歷史，見上方詳述；若之後改變決定再處理
- **本週**（🟠）：
  1. ~~`/auth/login`、`/auth/register` 加 rate limit~~（R-SEC-002，已修正）
  2. 加 `frontend/src/middleware.ts` 做 server-side 路由保護（R-FE-008）
  3. 使用者自助登入/改密碼/PIN 補審計紀錄（R-PII-003）
  4. ~~複合索引補 `postgresql_concurrently`~~（R-DB-008，已修正）
- **有空**（🟡/🔵）：其餘 9 條 🟡 + 5 條 🔵，依專案節奏排入之後版本。

## 5. 已跳過分區

無。ENV / FE / BE / DB / CACHE / SEC / PII 七區皆有對應訊號（`.env*`、`frontend/package.json` 含 next、`backend/pyproject.toml` 含 fastapi、`alembic/` + sqlalchemy、redis 於 pyproject 與 compose、B+C 存在故 SEC 執行、`users.email` 故 PII 執行），全數執行。

## 6. AD-xxx（專案特定問題）

### AD-001 缺備份/還原演練紀錄
- 檔案：`docs/Arch/backup-restore-log.md`（不存在）
- 內容：`docs/Arch/README.md` 定義此檔用於記錄還原演練（→ CICD-087），但檔案從未建立，代表從未實際跑過一次 `pg_dump`/還原流程並記錄時間。`docs/Arch/adr/0004-read-path-capacity-planning.md` 的 Consequences 章節已記錄此落差（估算資料量成長到個位數 GB 後，實際 RTO 未知）。
- 誰會痛：真的發生資料損壞/誤刪需要還原時，才會發現流程沒跑過、預期還原時間是猜的，屆時代價是「資料庫離線時間 = 未知」。
- 修正：實際跑一次 `pg_dump` + 還原到乾淨環境的演練，記錄耗時與步驟到 `docs/Arch/backup-restore-log.md`。

### AD-002 全套件 pytest 有既有 `Event loop is closed` flakiness，且並非只在整批跑才出現
- 檔案：`backend/tests/services/test_recurring_service.py`（5 個測試）、`backend/tests/test_health.py`、`backend/tests/api/test_admin.py`、`backend/tests/api/test_liabilities.py`
- 內容：實作 R-SEC-002 後跑全套件驗證時，這 4 個檔案穩定出現 `RuntimeError: Event loop is closed` / `attached to a different loop`（asyncpg 連線關閉時卡到已關閉的 event loop），與這次改動內容（auth/rate limit/redis）無關聯（`test_recurring_service.py` 完全不碰 auth）。`docs/Tasks/v1.1.0/tasks-v1.1.0.md` 原本記載「整批跑才會出現，單獨跑會過」，但這次額外驗證發現 **`test_recurring_service.py` 單獨跑（不與其他檔案同批）依然出現相同錯誤**，代表既有認知可能已經過時（版本升級後惡化，或原本的判斷就不夠全面）。
- 誰會痛：CI／本機驗證的訊噪比下降，每次跑批次測試都要人工判斷這 4 個檔案的紅燈是不是「已知的、可忽略的」，長期下來容易掩蓋真正的新迴歸。
- 修正：這是 `backend/tests/conftest.py` 的 `engine`/connection pool 跨 event loop 重用問題（`pytest-asyncio` 每個測試函式建立新 event loop、但 SQLAlchemy `engine` 是模組層級單例），需要重新設計 test engine 的生命週期（例如改成 function-scoped engine，或改用 `NullPool` 搭配每測試獨立連線）；範圍較大且跟本次改動無關，建議另開版本任務處理，不在本次 R-SEC-002 修正範圍內動 `conftest.py`。

其餘已巡視面向：race condition（`_adjust_account_balance` 用原子 `UPDATE ... SET balance = balance + :delta`，非 load-and-mutate，無需額外檢查）、狀態機（交易 transfer 雙分錄的 CHECK constraint 已在 DB 層擋形狀互斥）、啟動流程（`Settings._fail_fast` 已檢查關鍵 secret）、多表寫入 transaction 邊界（recurring/liability repay 已用 `db.begin()`）——皆未發現額外問題，不重複列出。

## 7. 規範自身問題

無。本次掃描未發現 `rules/` 之間矛盾、缺漏，或掃描規則的「對應規範」仍為 `?` 佔位的情況。
