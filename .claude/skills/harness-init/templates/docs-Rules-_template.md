---
area: PROJ
file: 01-example
title: <模組名>
load_on: [<關鍵字>]
rules: [PROJ-001]
---

# <模組名>

> 複製本檔為 `NN-<主題>.md`，改 `file` / `title` / `load_on` / `rules`；ID 連號、全專案唯一；引用 harness 規則寫 `→ <AREA>-NNN`。`_` 開頭檔不會被載入。

- **PROJ-001** <規則正文；必 / 禁 措辭，附可驗證判準>
